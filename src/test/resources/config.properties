# Swap this to your own deployed API once ready, e.g.:
# base.url=https://credit-risk-api.onrender.com/api/v1
base.url=https://reqres.in/api

# reqres.in requires an API key + project ID on every request.
# Get yours at https://app.reqres.in/?next=/api-keys
# Both are read from env vars (REQRES_API_KEY, REQRES_PROJECT_ID) via ConfigReader.
# Leave these as placeholders — never commit real values.
api.key="pro_c8f87cf1d533afd18e9bb2136b67ed2f5b640a99f7306f50afcb8e9aefaaaa92"
project.id="40515"

# request timeout (ms)
request.timeout=10000